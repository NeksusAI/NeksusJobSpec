# Minimal Theme Example

A complete minimal custom theme package for local development.

## Validate

```bash
neksus-jobspec themes validate examples/themes/minimal
```

## Render with it

```bash
neksus-jobspec spec render examples/startup-engineer.jobspec.yaml --format web --theme examples/themes/minimal --output dist/startup-engineer-minimal.html
```
